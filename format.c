#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int space_counter(str_arr[], int i)
{

        i++;
}

format_str(str_arr[])
{
    char check_arr[];
    int i, j, k;                    /*i -  номер символа, который проверяем
                                        j - кол-во символов в выходном массиве*/
    space_counter(str_arr, i);      

}